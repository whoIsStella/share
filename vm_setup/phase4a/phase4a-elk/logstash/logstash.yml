node.name: hive-logstash

path.config: /usr/share/logstash/pipeline

xpack.monitoring.enabled: false  # suppresses monitoring warning on free tier

log.level: info  # set to debug if pipelines are misbehaving
